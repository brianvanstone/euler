package tech.notpaper.euler;

import static org.junit.Assert.assertEquals;

import tech.notpaper.euler.util.math.PrimeUtils;
import tech.notpaper.euler.util.math.PrimeUtils.PollardRho;

public class Euler3 {
	
	/**
	 * Temporarily implemented a nieve solution until defects in
	 * {@link PollardRho} can be resolved
	 * 
	 * @param n the number to factor
	 * @return the largest prime factor of n
	 */
	public static long largestPrimeFactorOf(long n) {
		
		
		return -1;
	}
	
}
